export interface AircraftProfile {
  tailNumber: string;
  serialNumber: string;
  eventTitle: string;
  publishedOn: string;
}

export interface BinderSection {
  key: string;
  label: string;
  accent: string;
  summary: string;
}
